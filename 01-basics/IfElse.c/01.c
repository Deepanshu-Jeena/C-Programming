#include<stdio.h>
int main(){

    //check if the number is +ve or -ve
    int x;
    printf("Enter your Number :");
    scanf("%d",&x);
    if(x>=0){

        printf("your number is positive, which is %d",x);
    }
    else{

        printf("Your number is negative, which is %d",x);
    }
    





    return 0;
}